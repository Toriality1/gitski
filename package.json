{
  "name": "gitski",
  "author": "toriality",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "gitski": "./dist/index.js"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --banner:js=\"#!/usr/bin/env node\"",
    "dev": "npm run build && node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.27.2",
    "typescript": "^5.0.0"
  }
}
